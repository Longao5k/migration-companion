# 第一阶段续建评审记录

本文件记录接手开发后每个交付门（Stage Gate）的产品经理与技术总监评审。评审对象是
[`docs/product/product-manager-review.md`](../product/product-manager-review.md) 冻结的 P0 与验收口径，
以及 [`docs/architecture/technical-director-review.md`](../architecture/technical-director-review.md)
的架构与安全边界。交接清单见 [`docs/handoff/claude-handoff-2026-08-27.md`](../handoff/claude-handoff-2026-08-27.md)。

评审规则：

- 只有可重复执行、有输出证据的结果才算通过。页面存在、代码路径预留、本地沙盒成功都不算。
- 任何一方提出的问题必须落到具体门（本门修复 / 指定后续门 / 明确列入外部依赖阻塞）。
- 需要外部账号、商业授权或澳洲专业人士复核的门槛，不由本记录签核。

---

## 门 M1：恢复绿色基线并自动化既有手工验收（P0-A）

**日期：** 2026-08-27
**范围：** 交接文档第 5 节 P0-A 第 1–4 条。

### 交付内容

| 项 | 说明 |
| --- | --- |
| `services/api/src/bootstrap.ts` | 抽出 `configureApp`，`main.ts` 与端到端验收共用同一套全局前缀、校验管道和 helmet 配置。 |
| `services/api/test/jest-e2e.json` | 补上 `test:e2e` 指向但缺失的配置，不再需要 `--passWithNoTests` 掩盖。 |
| `services/api/test/acceptance.e2e-spec.ts` | 把交接文档第 4 节的手工验收固化为 18 条自动化验收。 |
| `services/api/test/local-e2e-env.ts` | 本地验收环境默认值；显式拒绝在 `NODE_ENV=production` 下运行。 |
| `scripts/test-local-e2e.ps1` | 一条命令完成容器启动、结构同步、桶准备与验收。 |
| `scripts/preview-web-release.ps1` | 构建并预览 Web release，附带固定 390x844 视口检查页。 |
| `scripts/lib/pnpm.ps1` | 统一解析 pnpm 调用方式，脚本不再假设本机装有全局 pnpm。 |

同门修复的缺陷：

1. **`GET /v1/projects/:projectId` 在项目含文件时返回 500。** `FileRecord.byteSize` 是 BigInt 列，
   `JSON.stringify` 无法序列化。已确认为真实缺陷：回退修复后重跑验收，18 条中 3 条失败，
   服务端日志为 `TypeError: Do not know how to serialize a BigInt`。修复后该接口同时不再返回
   `storageKey`，符合数据边界中“不把对象 key 当授权”的规则。
2. **PowerShell 脚本缺少 UTF-8 BOM。** Windows PowerShell 5.1 按 ANSI 读取，中文字符串会破坏解析，
   `.\scripts\start-local.ps1` 在 5.1 下直接语法报错。五个脚本已统一加 BOM。
3. **`APP_ORIGIN` 缺少 `http://127.0.0.1:53004`。** Flutter Web 预览调用本地 API 会被 CORS 拒绝。已补。

同门补齐的权限模型缺口（为 M2 的云文件删除 UI 做准备）：

4. `FileRecord.uploadedById` 与迁移 `20260827120000_file_uploader`；`DELETE /v1/files/:fileId`
   按冻结角色规则执行：所有者删除任何文件、可协作成员只能删除自己上传的文件、仅查看成员不能删除。

### 验收证据

| 命令 | 结果 |
| --- | --- |
| `pnpm --filter @migration-companion/api build` | 通过 |
| `pnpm --filter @migration-companion/api lint` | 通过 |
| `pnpm --filter @migration-companion/api test` | 2 suites / 4 tests 通过 |
| `.\scripts\test-local-e2e.ps1` | 1 suite / **18 tests 通过** |
| `fvm flutter analyze` | 0 issue |
| `fvm flutter test` | 6 tests 通过 |
| `fvm flutter build web --release` | 通过 |

Web 视口复测：release 产物在真实 Chrome 中以 390x844 iframe 视口首帧渲染正常，
刷新后仍正常渲染，未复现交接文档记录的 loader 停滞。结论：该现象来自 debug 的
`flutter run -d web-server` 调试连接，不是 release 行为。Android 模拟器复测仍待执行。

### 产品经理评审

**结论：通过。** M1 不新增任何用户可见能力，因此不计入第 5.1 节功能门槛的任何一条；
它的价值是把此前只存在于一次性手工操作中的验收变成可重复证据，这是第 5 节整体的前提。

自动化现已强制执行的冻结产品规则：

- 注册不等于允许上传：未明确开启云文件同意前上传被拒（4.1 第 5 步）。
- 7 天试用需用户明确开启、每账号一次、不创建商店订阅（6.4）。
- 安全分享默认禁止下载、必须访问码、错误访问码被拒、可撤销、撤销后立即失效（6.2）。
- 分享范围只包含用户逐项选择的内容；未选择备注时备注不下发（4.2 第 1 步、6.2）。
- 仅查看成员默认不能下载，只有所有者开启项目级开关后才可以；仅查看成员不能评论（6.3）。
- 可协作成员可改清单、可评论；降权后立即失去写权限（4.3 第 5 步）。
- 并发编辑返回 409，不做“最后写入者静默覆盖”（6.1）。
- 客户端购买凭证本身不授予权益；只有服务端核验事件能变更权益；退款/撤销立即收回（6.1、7.3）。
- 模板条目带官方来源 URL 与复核日期（5.3）。
- 审计记录不含文件名与存储位置（5.2）。

**要求补齐的验收缺口**（不阻塞本门，指定到后续门）：

| 缺口 | 冻结依据 | 指定门 |
| --- | --- | --- |
| 分享有效期 1/14/30 天分支、访问码连续错误锁定 | 6.2 | M2 |
| 云容量 1 GB / 10 GB 上限拒绝与超额只读 | 6.1、6.4 | M3 |
| 账号删除请求、7 天主数据清除、备份轮换 | 5.2 | P0-G（外部基础设施） |
| DOC/DOCX 兼容等级与失败保留原件 | 2.6、5.1 | P0-B（外部授权阻塞） |
| 访客离线创建、备份导出→删除→导入恢复演练 | 5.1 | M3（客户端自动化） |

**风险提示：** 当前验收全部走 `DEV_AUTH` 开发身份，不能证明生产身份链路。产品经理不接受把
本门结果表述为“账号体系已验收”。

### 技术总监评审

**结论：通过。**

- **接受** `configureApp` 抽取：验收跑在与部署一致的 HTTP 管道上，避免出现“测试通过但生产管道不同”的假绿。
- **接受** BigInt 修复方式：在服务层显式转换并收窄 `select`，与 `files.service` 既有约定一致，
  优于全局修改 `BigInt.prototype`——后者会让未来任何新接口静默改变类型而不被发现。
- **接受** 上传者字段与删除接口：此前 `FileRecord` 没有上传者，无法执行“不能删除他人文件”，
  这是权限模型缺口而不是 UI 缺口。删除接口先落库再删对象，对象清理失败会留下
  `file.object_delete_failed` 审计并返回可重试错误，不会出现“记录还在但字节已消失”的反向不一致。
- **拒绝** 在脚本中默认关闭 corepack 完整性校验。`scripts/lib/pnpm.ps1` 只在检测失败时报错并列出
  三种修复方式，其中跳过校验必须由操作者显式选择。供应链校验不因为本机方便而默认关闭。
- **记录** 迁移与 `db push` 的分工：本地脚本继续用 `prisma db push`；生产部署必须用
  `prisma migrate deploy`，`20260827120000_file_uploader` 已按初始迁移的格式补齐。
- **记录** 验收数据清理：套件用一次性随机邮箱，结束时按邮箱级联删除账号；文件对象在
  隔离区→clean 拷贝和删除测试中被清理，MinIO 不留残留对象。
- **遗留到 P0-C：** 验收未覆盖 Cognito 校验路径、令牌刷新与多设备。
- **遗留到 P0-G：** 开发扫描模拟仍未替换为独立扫描 Worker；`DEV_AUTO_SCAN` 只在非生产可用，
  但生产替换尚未开始。

---

## 门 M2：App 内文件、分享与协作管理界面（P0-D）

**日期：** 2026-08-27
**范围：** 交接文档第 5 节 P0-D 第 1–5 条，以及 M1 评审中产品经理指派到本门的验收缺口。

### 交付内容

| 项 | 说明 |
| --- | --- |
| `apps/mobile/lib/features/projects/project_cloud_screen.dart` | 新增“云端与协作管理”界面：云文件、安全分享、协作三个页签。 |
| `apps/mobile/lib/core/state/app_store.dart` | 新增分享列表/撤销、协作读取/改角色/移除/撤销邀请、讨论读取与发表、云文件列表/状态刷新/删除/下载。 |
| 安全分享创建对话框 | 现在列出已通过扫描的云文件并逐项选择；读取失败时显示原因，仍允许只分享清单项。 |
| “直接发送副本” | 现在把选中的本机附件作为 `XFile` 交给系统分享面板，并在选中文件时给出不可撤回的红色提示。 |
| `DELETE /v1/files/:fileId` | 按冻结角色规则删除云文件（M1 已落地，本门接入 UI）。 |

同门修复的缺陷：

1. **“创建安全分享入口”静默失败。** 分享面板的 `ListTile` 在 `Navigator.pop(context)` 之后，
   把**面板自己的** `BuildContext` 传给后续流程。该 context 随路由卸载，`context.mounted` 变为 false，
   于是创建流程直接 return：既不创建入口，也不显示错误。已改为使用项目页的 context。
   回归测试 `apps/mobile/test/secure_share_flow_test.dart` 在修复前失败（未发出任何请求）、修复后通过。
2. **“安全入口已创建”对话框无法关闭。** 只有“复制访问码”（不关闭）和“发送链接”（关闭并调起系统分享）
   两个动作，用户复制访问码后没有退出路径。已补“完成”。

### 验收证据

| 项 | 结果 |
| --- | --- |
| `fvm flutter analyze` | 0 issue |
| `fvm flutter test` | 12 tests 通过（新增 5 条云文件用例 + 1 条分享流程回归） |
| `.\scripts\test-local-e2e.ps1` | 21 tests 通过（新增有效期分支、访问码锁定、含文件分享、删除权限） |
| 真实 Chrome + Web release + 本地全栈 | 见下 |

浏览器端到端实测（Chrome，release 产物，连本地 API/PostgreSQL/MinIO）：

1. 邮箱登录成功（修复 CORS 之前该步骤直接失败）。
2. 创建 SA 190 项目；未开启云同步时“云端与协作管理”正确显示“此项目仅保存在本机”。
3. 明确开启项目云同步成功。
4. 三个页签均从服务端加载：云文件（空状态）、安全分享（空状态）、协作（显示所有者、角色、
   “所有权不能通过邀请或角色调整转移”、讨论区隐私提示）。
5. 创建安全分享入口：默认 7 天、随机访问码、两个开关默认关闭、未选内容时“创建”禁用；
   创建后返回带 fragment secret 的链接与访问码。
6. 安全分享页签显示“有效 / 禁止下载 / 内容：1 个清单项、0 个文件 / 到期 2026年9月3日 / 尚未被访问”。
7. 点击“立即撤销”→ 二次确认 → 状态变为“已撤销”，撤销按钮禁用，列表从服务端重新拉取。

### 产品经理评审

**结论：通过（不含未覆盖项）。**

现在真正闭环、可以计入第 5.1 节功能门槛的部分：

- 分享者可查看有效/已过期/已撤销状态、到期时间与最近访问时间，并可立即撤销（4.2 第 5 步、6.2）。
- 分享范围默认全不选，用户逐项选择清单与已扫描文件；备注需单独勾选（4.2 第 1 步）。
- 直接发送副本现在真的发送文件，并在选中文件时明确提示无法远程收回（4.2 第 2 步）。
- 所有者可看到成员与角色、调整角色、移除成员、撤销未接受的邀请（4.3 第 5 步、6.3）。
- 云端副本可删除，且明确说明“不影响这台设备上的原件”，与“不得用付费换回自己的文件”一致（6.1）。
- 讨论区带“不要填写证件号码、访问码或政府表格答案”的提示，符合第 7.1 节边界。

M1 指派到本门的缺口：分享有效期 1/14/30 天分支与访问码连续错误锁定**已补齐自动化验收**；
“永久链接”被服务端拒绝（400）也已验证。

**本门未覆盖、继续指派：**

| 缺口 | 冻结依据 | 指定门 |
| --- | --- | --- |
| 原件/编辑副本版本链 UI（`isOriginal`/`parentFileId` 目前无写入方） | 2.5、2.6 | P0-B（等真实编辑闭环） |
| 扫描状态自动轮询与失败自动重试（当前为手动刷新） | 5.1“失败状态” | M3 |
| 云容量 1 GB / 10 GB 上限与超额只读体验 | 6.1、6.4 | M3 |
| 访客离线创建、备份导出→删除→导入恢复演练自动化 | 5.1 | M3 |
| Android 真机/模拟器上验证本门界面 | 5.4 | 下一门 |

**风险提示：** 本门全部在 Chrome Web release 上验证。网页预览不持久化敏感附件，因此
“直接发送本机文件”和“下载云文件到 App 私有目录”两条路径在 Web 上必然为空或报错，
**必须在 Android/iOS 上另行验收**，不能用本门结果代替。

### 技术总监评审

**结论：通过。**

- **接受** 三个页签一律直接读服务端、不写入本机项目状态的做法。撤销分享、降权成员和删除文件之后，
  本机缓存会让用户误判当前权限；这里的正确性优先于渲染速度。
- **接受** `_RemoteSection` 把“加载 / 失败可重试 / 空状态”统一成一个骨架，并且**失败显示为失败**，
  不退化成看起来正常的空列表——这正是交接文档禁止的“页面存在即完成”反模式。
- **接受** 刷新失败不再抛回调用方：否则“删除已成功但刷新失败”会被错报成“删除失败”，
  用户会重复执行破坏性操作。
- **接受** 下载云文件时校验 SHA-256：不一致即丢弃，且不覆盖本机原件。这条对应残余风险表里的
  “不允许静默损坏”。
- **接受** 为测试注入 `ApiClient` 工厂：此前所有服务端调用都在方法内部 `new`，客户端逻辑完全无法测试。
  现在 5 条云文件用例和 1 条分享流程回归都跑在 `MockClient` 上，不需要真实网络。
- **记录** 缺陷 1 的类别：**弹出层关闭后继续使用其 BuildContext**。这类问题不会被 analyze 发现，
  也不会报错，只会静默不执行。已在同类位置检查：材料项详情面板已正确使用 `sheetContext`，
  只有分享面板存在该问题。
- **要求并已修正：** `DELETE /v1/files/:fileId` 最初先删数据库记录再删对象。评审要求反过来：
  记录先消失、对象清理再失败的话，会留下用户看不见、也无法重试的残留字节，而用户已经被告知文件已删除。
  这与“删除必须是真实删除”的隐私门槛冲突。现在改为**先删对象、成功后才删记录**；
  对象删除失败时记录保留、返回可重试错误，`DeleteObject` 对不存在的 key 幂等，重试安全。
- **遗留：** `refreshCloudFileStatuses` 每次都拉取整个项目详情（含清单与审计事件）来读文件列表。
  文件规模变大后应改为独立的 `GET /projects/:id/files`；当前 P0 规模下可接受。
- **遗留到 P0-D 剩余项：** multipart 直传仍在 API 内存中承载最多 50 MB，生产应优先走预签名直传。

---

## 门 M3：本机恢复、云容量与扫描反馈闭环

**日期：** 2026-08-28
**范围：** M1/M2 指派的备份恢复、云容量与扫描状态缺口。

### 交付与证据

- 完整 v2 备份已自动化验证“导出 → 新仓库/新附件目录 → 导入”：项目、清单状态、提醒、附件字节和
  SHA-256 全部恢复；导入时主动清除旧账号、远端 ID 和分享下载权限，防止备份误绑定原云项目。
- 容量判断同时计入已存文件和未完成上传预留；超额后只禁止新增上传，下载、导出和删除仍可用。
- 云文件扫描为 `PENDING` 时界面自动轮询；失败与超时显示为失败，不伪装成空状态或成功。
- Flutter 完整套件现为 18 tests；API 端到端第 22 条覆盖预留容量与超额只读。

### 产品经理评审

**结论：通过。** 用户在断网、本机迁移和云空间不足时仍能取回自己的数据；注册/导入不会静默上传或
继承旧云权限。M1/M2 指派到 M3 的三个缺口均已有可重复证据。

### 技术总监评审

**结论：通过。** 恢复流程逐文件校验 hash 后才写入新私有路径；云配额以“已存 + 已预留”统一判断，
避免并发上传穿透免费额度。轮询有页面生命周期与终态边界，不会无限后台请求。

---

## 门 M4：内容运营闭环与可靠同步（P0-C/P0-E 本地可完成部分）

**日期：** 2026-08-28
**范围：** 不依赖生产云账号、商店账号或商业文档授权的内容与同步能力。

### 交付内容

| 领域 | 已完成 |
| --- | --- |
| App 内容 | `/content/news`、`/content/changes` 拉取，JSON 离线缓存，加载/空/失败/缓存状态，收藏保持。 |
| 内容 API | 来源注册、新闻草稿/发布、变化审核/更正、标签、采集健康和公开发布过滤。 |
| 运营后台 | 审核队列、已发布内容、官方来源、更正记录、采集状态均连接真实 API；密钥只保存在当前内存会话。 |
| 爬虫证据 | 每次成功、未变化和失败均上报健康；快照 key/hash 登记；失败只上报错误类别，不回传敏感响应正文。 |
| 离线同步 | 本机先保存、持久化操作队列、稳定 operation ID、服务端事务幂等、自动重试、云项目拉取与换机恢复。 |
| 冲突 | 409 不静默覆盖；只显示状态/备注/日期等字段名，不展示备注正文；用户明确选择云端或本机重放。 |

### 验收证据

| 验收 | 结果 |
| --- | --- |
| `fvm flutter analyze` | 0 issue |
| `fvm flutter test` | **18 tests 通过**，含内容缓存、完整备份、断网重启队列、409 和换机恢复 |
| API build / lint / unit | 通过；2 suites / **4 tests** |
| `scripts/test-local-e2e.ps1 -StopStack` | 1 suite / **25 tests 通过** |
| Admin / Web build + lint | 通过 |
| Crawler unittest | **7 tests 通过** |
| Flutter Web release / Android Debug APK | 构建通过 |
| 页面实测 | 后台五模块真实加载；390×844 release 首帧正常；Pixel Fold API 35 安装启动、动态内容加载、无 App 崩溃 |

### 产品经理评审

**结论：通过（仅限本地可交付范围）。** App 不再用 SeedData 冒充实时内容；新闻草稿不会泄漏到公开 API，
重要/重大变化没有编辑摘要不能发布，更正记录不删除原证据。离线编辑和换机恢复已成为用户可理解的闭环，
冲突选择不会暴露敏感备注内容。

本门不签核生产身份、远程推送或商店订阅。收藏、关注和提醒跨设备以及 APNs/FCM 仍需后续生产服务。

### 技术总监评审

**结论：通过。** 同步操作与项目版本递增、清单修改和审计在同一 PostgreSQL 事务提交；相同 operation ID
与相同 payload 返回原结果，不同 payload 复用 ID 返回 409。内容候选使用确定性内容 hash 生成 ID，
重试不会生成重复审核项；发布过滤位于服务端，客户端不能绕过人工审核门。

Android 模拟器首次冷启动约 25 秒，日志显示主要时间花在 389 MB 评估版 PDF SDK 资源解压和安全存储迁移。
这不阻塞内部 Debug 验收，但阻塞性能签核；自研文档 SDK 替换后必须重测 APK/IPA 体积、首开和低端设备内存。

### 明确遗留

- 生产 Cognito 邮箱验证、token 刷新和多设备身份验收。
- APNs/FCM、关注规则、Outbox/SQS 和人工核实后的远程通知。
- 生产 evidence bucket/WORM 策略、调度、故障告警和人工审核 SLA。
- 预签名移动直传替换当前最多 50 MB 的 API multipart 路径。
- iOS 真机与 Android 手机尺寸真机矩阵；本门只完成 Android 折叠屏模拟器。

---

## 门 M5：上传、通知与账号生命周期收口

**日期：** 2026-08-28
**范围：** 第一阶段不依赖生产账号即可完成的上传可靠性、通知偏好、账号删除和隐私清理。

### 交付与证据

| 领域 | 已完成 |
| --- | --- |
| 预签名上传 | App 直传对象存储；本机只保存 upload session ID；丢失完成响应后复用会话，不重复上传字节；服务端完成接口幂等。 |
| 隔离区清理 | 10 分钟未完成会话由 worker 先删对象再删会话；真实 MinIO 对象不存在验证通过。 |
| 通知规则 | 账号级 SA/联邦、190/491、仅重要变化偏好；核实/更正与 Outbox 同一数据库事务；去重、认领超时和有限重试。 |
| 通知隐私 | 锁屏 payload 只有通用标题和路由，不含正文、编辑摘要、来源或用户资料。 |
| 账号删除 | 7 天撤回；到期批处理；文件对象先删、数据库后删；保留不含邮箱/文件名的 HMAC 最小证明。 |
| 构建与验收 | Flutter analyze 0、21 tests；API unit 4、e2e 29；crawler 7；Admin/Web build+lint；Flutter Web 和 Android release APK 构建；pnpm production audit 0 known vulnerabilities；空数据库成功顺序应用 7 个生产迁移。 |

### 产品经理评审

**结论：通过（第一阶段本地可交付范围）。** 用户可以看懂上传是否完成、在完成响应丢失后安全继续；可以选择
政策关注范围；账号删除不会立即清空本机项目，且在明确的 7 天窗口内可撤回。超额、待扫描和删除中的数据都
保留取回/重试路径。通知只针对已人工核实的重要变化，未把自动抓取候选直接推给用户。

生产 APNs/FCM 投递、商店购买真实性和云备份删除证明没有被本门伪造为完成，继续作为公开发布硬门。

### 技术总监评审

**结论：通过。** 上传会话把容量预留、对象 hash/大小/类型/魔数校验和文件记录提交拆成可恢复阶段；重复 complete
返回同一记录。过期隔离对象与账号文件都遵循“对象删除成功后才删可重试记录”。删除 ledger 的生产 HMAC 密钥在
任何对象删除前校验，避免配置错误造成数据库仍在但字节已丢失。通知发布和 Outbox 同事务，确定性 dedupe key
阻止同一状态重复投递；worker 只接收非敏感错误类别。

Android release 构建通过；R8 对 OkHttp 可选 Conscrypt 探测使用构建工具给出的精确 `-dontwarn` 规则，未关闭压缩。
当前 APK 243.2 MB，主要来自评估版文档 SDK，**只满足内部开发验收**。正式签名、商店包、体积/首开性能和许可
必须在自研 SDK 替换后重新签核。

详细数据流、威胁与残余发布门见 [`../architecture/security-privacy-review.md`](../architecture/security-privacy-review.md)。

---
