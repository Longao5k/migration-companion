.fvm/flutter_sdk
.fvm/versions
.fvm/fvm_config.json
.fvm/release
.fvm/version
.dart_tool/
.idea/
.vscode/
**/.dart_tool/
**/build/
**/.flutter-plugins
**/.flutter-plugins-dependencies
node_modules/
.env
.env.*
!.env.example
coverage/
dist/
*.log
.local/
__pycache__/
*.py[cod]
**/.kotlin/

# 抓取证据与采集器本地状态：包含官方页面原始 HTML 与规范化全文。
# 按冻结的版权与证据规则，这些只能留在独立的私有证据存储，不得作为自有内容提交或再发布。
.local-live-check/
.local-state/

# Android 发布签名：密钥与口令由产品所有者保管，绝不进版本库。
# 丢了 upload key 就无法再向 Google Play 更新同一个应用。
apps/mobile/android/key.properties
*.jks
*.keystore

# 摘要工具的产出与本地密钥，永远不进仓库
summaries*.json
*.env.local
.env.summarizer
